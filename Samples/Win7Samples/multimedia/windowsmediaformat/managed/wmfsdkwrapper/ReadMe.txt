========================================================================
       Windows Application : ManagedWMFSDKWrapper
========================================================================

This managed-code assembly serves as a wrapper for accessing the WMFSDK functions from a managed-code base. It needs to be built before the managed samples can be built.